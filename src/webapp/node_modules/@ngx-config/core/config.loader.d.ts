export declare abstract class ConfigLoader {
    abstract loadSettings(): any;
}
export declare class ConfigStaticLoader implements ConfigLoader {
    private readonly providedSettings?;
    constructor(providedSettings?: any);
    loadSettings(): any;
}
